/**
 * <h3>Viewer settings</h3>
 * 
 * @
 * 
 * @namespace Cortona3DSolo.uniview.settings
 * @prop {boolean} Settings=false Displaying the settings panel.
 * @prop {boolean} MeasureTool=false Display of the "Measurement" tool.
 * @prop {boolean} SectionPlane=false Display of the "Section plane" tool.
 * @prop {boolean} NavigationCube=false Displaying the navigation cube.
 * @prop {number} [NavigationScheme=0] The navigation scheme (0 - Default, 1 - Aircraft, 2 = Multiview projection).
 * @prop {number} SceneLighting=0 Scene lighting (0 - Default, 1..6 - Scene lighting scheme 1..6).
 * @prop {boolean} ShowSurfaceEdges=true Show surface edges.
 * @prop {boolean} AntiAliasing=false Anti-aliasing.
 * @prop {boolean} AmbientOcclusion=false Ambient occlusion.
 * @prop {number} SelectionMode=0 Selection mode (0 - Highlight with color, 1 - X-ray selected objects, 2 - Translucent shell).
 * @prop {boolean} OutlineHoveredObjects=false Outline hovered objects.
 * @prop {boolean} SetCenter=false Displaying the filter panel.
 * @prop {boolean} ShowFilter=false Displaying the filter panel.
 * @prop {boolean} ExpandMain=false Displaying the "Main" panel only.
 * @prop {boolean} ExpandAux=false Displaying the "Aux" panel only.
 */

/**
 * <h3>Viewer settings: Catalog</h3>
 * 
 * @namespace Cortona3DSolo.uniview.settings
 * @prop {boolean} FullTable=false Displaying the entire DPL table.
 * @prop {number} SheetTransitionRate=1 Transition animation rate (0.5, 1, 2, 4, 8, 16).
 * @prop {boolean} TransitionAnimationControl=false Enabling transition animation control.
 * @prop {number} [BackgroundObjectsRenderingMode=0] Background objects rendering mode (0 - Default, 1 - X-ray, 2 - Translucent shell).
 * @prop {string} Sheet ID of the current catalog sheet.
 * @prop {boolean} DrawingDisplayMode=false Displaying 2D graphics for the current sheet.
 * @prop {boolean} SkipAnimation=false Skipping the transition animation.
 * @prop {boolean} SelectedObjectsExternalView=false Displaying selected objects in a separate window.
 * @prop {boolean} IgnoreTransparency=true Ignore transparency on click.
 * @prop {boolean} SetCenter=false The mode of setting the center of rotation.
 * @prop {boolean} ShowRotationCenter=false Displaying the center of rotation.
 * @prop {boolean} ShowAxes=true Displaying the axes.
 * @prop {string} [Filter{name}="*"] Applying a filter with the name.
 */

/**
 * <h3>Viewer settings: Procedure</h3>
 * 
 * @namespace Cortona3DSolo.uniview.settings
 * @prop {boolean} Comment=false Displaying inline comments for the procedure step.
 * @prop {number} PlaybackSpeed=1 Playback speed (0.5, 1, 2, 4).
 * @prop {boolean} FreezeCamera=false Freeze viewpoint.
 * @prop {boolean} Locked=false One step playback.
 * @prop {boolean} DisableAlertMessages=false Disable alert messages.
 * @prop {boolean} Comment=false Displaying inline comments for the procedure step.
 * @prop {boolean} SelectedObjectsExternalView=false Displaying selected objects in a separate window.
 * @prop {boolean} IgnoreTransparency=true Ignore transparency on click.
 * @prop {boolean} ShowFilter=false Displaying the filter panel.
 */

/**
 * <h3>Viewer settings: RWI</h3>
 * 
 * @namespace Cortona3DSolo.uniview.settings
 * @prop {boolean} PMI=false Displaying PMI.
 * @prop {number} Mode=0 Mode (0 - Preview, 1 - Job)
 */

/**
 * <h3>Viewer settings: Training</h3>
 * 
 * @namespace Cortona3DSolo.uniview.settings
 * @prop {boolean} HighlightParts=false Enable extra-highlighting of parts in Demo and Study modes.
 * @prop {boolean} ContinuousMode=false Continuous playback in the Demo mode.
 * @prop {boolean} DirectHints=false Enable direct hints in the Study mode.
 * @prop {boolean} Spin=true Spin navigation mode.
 * @prop {boolean} Pan=false Pan navigation mode.
 * @prop {boolean} Zoom=false Zoom navigation mode.
 */ 
 
/**
 * <h3>Viewer settings: Book (DITA Map, S1000D PM)</h3>
 * 
 * @namespace Cortona3DSolo.uniview.settings
 * @prop {boolean} NavigationPanel=true Displaying the table of contents in the publication structure.
 * @prop {string} [FilterPCT] Applying the PCT filter.
 */ 
 
